package submission_draw_rect

// TASK["Draw a Rectangle"]
// MAX_SCORE[5]
// STUDENT["John Doe"]

/* SUBMISSION

// Expects two numbers `height` und `width`.
// Draws a rectangle with these dimensions to the console.
// The recangle is completely filled with the symbol '#'.
func DrawRectangle(height, width int) {
	// BUG[-1]: Use `:=` to declare the variable.
	for foo = 0; foo < height; foo++ {
		for bar := 0; bar < width; bar++ {
			fmt.Print("#")
		}
		//BUG[-2] Start a new line here.
	}
}

SUBMISSION_END */

// REMARKS
// - Please use better names for the variables, e.g. `row` and `col`.
